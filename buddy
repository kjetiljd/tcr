#!/usr/bin/env bash

watch_path="${TCR_WATCH_PATH:-src}"

while true; do
  if ! fswatch -1 "${watch_path}"; then
    echo "*** fswatch failed for ${watch_path}. Install with 'brew install fswatch'." >&2
    exit 1
  fi

  tcr
done
